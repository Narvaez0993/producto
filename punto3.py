#Generar un diccionario con los datos de un departamento de Colombia (código, nombre, poblacion, municipios (3 municipios). 
#Luego, mostrar este diccionario, solo con el segundo municipio.

"""Departamentos = {
    'codigo' : '04',
    'nombre' : 'sebas',
    'poblacion': '10000',
    'municipios':['rionegro','medellin','bello']
}

lista = Departamentos.get('municipios')
pocision = lista.index('medellin')

for clave, valor in Departamentos.items():
    if(valor == 3):
        for i in range(len(lista)):
            if(i == pocision):
                valor = lista[i]
                print(valor)
    if(Departamentos):
        print (clave,":", valor)"""


    

"""Departamentos = {
    'codigo' : '04',
    'nombre' : 'sebas',
    'poblacion': '10000',
    'municipios':['rionegro','medellin','bello']
}

Departamentos['municipios'] = Departamentos['municipios'][1]

for clave, valor in Departamentos.items():
     if(Departamentos):
        print (clave,":", valor)"""
